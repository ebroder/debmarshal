# -*- python-indent: 2; py-indent-offset: 2 -*-
"""debmarshal autoconf configuration

This module interprets and provides the values from debmarshal's
autoconf configuration for use in Python code.

In particular, this is designed to extract the values of such
variables as @libexecdir@, which incorporate other variables in
shell-like substitutions.
"""

__authors__ = [
    'Evan Broder <ebroder@google.com>',
]

import string

def evalVariable(val):
  """Interpolate variables using a shell-like syntax

  Args:
    val: A string that might have ${variables} in it

  Returns:
    A string with the variables substituted, based on other variables
      that have been defined in this module
  """
  return string.Template(val).substitute(**globals())

prefix = evalVariable('@prefix@')
exec_prefix = evalVariable('@exec_prefix@')
libexecdir = evalVariable('@libexecdir@')
